services:
  - type: web
    name: gotenberg-valrano
    runtime: docker
    repo: https://github.com/gotenberg/gotenberg
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: GOTENBERG_API_PORT
        value: "10000"
    healthCheckPath: /health
